import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Internship.sg | AI Career Coach for Singapore Students",
  description: "Get personalized internship guidance, AI mock interviews, resume tips, and career recommendations for Singapore students.",
  keywords: "internship singapore, career coach, mock interview, resume tips, cover letter, student jobs",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
